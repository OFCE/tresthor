% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_1_sparse_solver.R
\name{thor_solver_sparse}
\alias{thor_solver_sparse}
\title{Solve a sparse thoR model}
\usage{
thor_solver_sparse(
  model,
  first_period,
  last_period,
  database,
  index_time = "date",
  convergence_criteria = 1e-10,
  atol = 1e-08,
  max_iter = 100L,
  damping = TRUE,
  cache = NULL,
  verbose = TRUE,
  diagnostics = FALSE
)
}
\arguments{
\item{model}{a \code{thoR.model} built by \code{create_model_sparse()}}

\item{first_period}{first period to solve, as found in \code{index_time}}

\item{last_period}{last period to solve}

\item{database}{data.frame holding the data}

\item{index_time}{name of the time column in \code{database}}

\item{convergence_criteria}{relative step tolerance (\code{rtol}). A block has
converged when every endogenous variable satisfies
\verb{|dx| <= rtol*|x| + atol}. Relative rather than absolute because macro
models mix variables of very different magnitude. Default 1e-10.}

\item{atol}{absolute step tolerance, which governs variables sitting at or
near zero. Default 1e-8.}

\item{max_iter}{maximum Newton iterations per block and period. Default 100.}

\item{damping}{boolean. Backtrack the Newton step when it would increase the
residual. Default TRUE.}

\item{cache}{directory in which to cache the compiled object between R
sessions, FALSE to disable, or NULL (the default) for
\code{tresthor_cache_dir()}.}

\item{verbose}{boolean. Print progress per period. Default TRUE.}

\item{diagnostics}{boolean. Return iteration counts and residuals as
attributes of the result. Default FALSE.}
}
\value{
the input data.frame with the endogenous variables solved.
}
\description{
Solve a sparse thoR model
}
