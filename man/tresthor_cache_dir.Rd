% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_11_compile_model.R
\name{tresthor_cache_dir}
\alias{tresthor_cache_dir}
\title{Directory used to cache compiled models between sessions}
\usage{
tresthor_cache_dir()
}
\value{
the cache directory, created if needed, or NULL if caching is off
}
\description{
Without a persistent cache, \code{Rcpp::sourceCpp()} builds into \code{tempdir()} and
so recompiles every model once per R session -- around 14 s for ThreeME
4x4 and 30 s for 8x8, and considerably more for larger models. With one,
the compilation happens once per machine and per version of the model.
}
\details{
Rcpp keys its cache on the hash of the source, the platform and its own
version, so an edited model or a changed toolchain rebuilds by itself. It
does not key on the R version, and R binaries are not compatible across
minor releases, so that is added here.

Set \code{options(tresthor.cache.dir = "...")} to move it, or
\code{options(tresthor.cache.dir = FALSE)} to disable caching entirely.
}
